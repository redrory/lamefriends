

<% if current_user %>
	Welcome Mr. <%= current_user.last_name %> | <%= current_user.location %> | <%= current_user.gender %> <br>
	My token <%= current_user.token %> || Passing token |_ <%= @rush_me_friends %>_|


	<%= image_tag(@my_pic) %>
	<%= link_to "Sign out", signout_path %>

    <%@rush_me_friends.each do |f| %>

    	<%= image_tag(@graph.get_picture(f["id"])) %>
    <% end %>

<% else %>
	<%= link_to "Sign in with Facebook", "/auth/facebook" %>

<% end %>